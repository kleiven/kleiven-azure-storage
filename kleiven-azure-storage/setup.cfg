[metadata]
name = kleiven-azure-storage
version = 0.0.1

[options]
python_requires = >=3.10
packages = find:
package_dir=
    =.
install_requires =
    python-dotenv
    azure-data-tables
    azure-storage-blob


[options.packages.find]
where = .
